{
  "name": "websocket_router",
  "version": "1.0.0",
  "description": "example showing how to run socket io and ws, onthe same node server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^1.4.3",
    "ws": "^1.0.1"
  }
}
